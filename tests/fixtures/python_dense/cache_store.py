def cache_put(key: str, value: str) -> None:
    """Store one cache entry under the given key."""
    del key, value


def cache_get(key: str) -> str | None:
    """Fetch one previously stored cache entry."""
    del key
    return None
